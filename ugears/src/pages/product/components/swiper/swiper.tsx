import swiperStyle from './swiper.module.scss'
import 'swiper/css';

type SwiperProps = {
    images_urls: string[],
    video_url: string
}

const Swiper: React.FC<SwiperProps> = ({ images_urls, video_url }) => {
    return (
        <div>

        </div>
    )
};

export default Swiper;
